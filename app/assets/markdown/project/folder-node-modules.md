## node_modules

项目依赖被安装在此目录中。因此，此目录应被排除在 `.gitignore` 中。

显然，**不应该**人为编辑此目录中的任何内容。所有更改应由包管理器完成。

::message{level=warning}
不同的包管理器（主要是 pnpm）对此目录中的依赖的组织方式不一样。因此，不建议在项目创建之后切换包管理器。
::

以全局模式（`-g`）安装的依赖不在此目录中。不过既然以全局模式安装，我们一般也不称呼这些东西为“依赖”。
