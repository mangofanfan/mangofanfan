## .vscode

::message{level=info}
这是 :term-tip-vscode 对该项目的配置文件目录。或称「工作区配置」。

所以如果你不使用 VS Code，则见不到此目录是正常的。
::

作为 VS Code 的项目配置目录，`.vscode` 中可以包含如下文件：

* `settings.json` - 工作区层级的设置；
* `tasks.json` - 工作区中可运行的任务；
* `launch.json` - 工作区中可进行的运行 / 调试；
* `extensions.json` - 工作区的推荐扩展列表；
* ……
